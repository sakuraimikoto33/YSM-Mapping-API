interface:
  display_name: "Rewrite YSM Mapping API History"
  short_description: "Safely rewrite YSM Mapping API Git history"
  default_prompt: "Use $rewrite-ysm-mapping-api-history for this explicitly requested history rewrite with backups and state comparison."

policy:
  allow_implicit_invocation: true
